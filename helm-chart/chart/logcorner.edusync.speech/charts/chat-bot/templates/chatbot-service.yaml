apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
  namespace: {{ .Values.namespace }}
  labels:
    app: chatbot

spec:
  selector:
    app: chatbot
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP

