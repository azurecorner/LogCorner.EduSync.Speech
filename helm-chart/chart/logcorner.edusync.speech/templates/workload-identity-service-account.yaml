apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    azure.workload.identity/client-id: {{  .Values.azureWorkloadIdentityClientId }}
  name: {{ .Values.serviceAccountName }}
  namespace: {{ .Values.namespace }} 