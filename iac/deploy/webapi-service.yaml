apiVersion: v1
kind: Service
metadata:
  name: webapi-service
  namespace: sre-agent-demo
  labels:
    app: webapi

spec:
  selector:
    app: webapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP

